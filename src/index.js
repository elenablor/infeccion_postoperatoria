import React from "react";
import ReactDOM from "react-dom/client";
import InfectionRiskApp from "./InfectionRiskApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <InfectionRiskApp />
  </React.StrictMode>
);
